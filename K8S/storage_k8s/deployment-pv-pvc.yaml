apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-exchange-pvc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shared-pvc-app
  template:
    metadata:
      labels:
        app: shared-pvc-app
    spec:
      containers:
      - name: busybox
        image: busybox
        command: ["/bin/sh", "-c"] 
        args: ["while true; do echo $(date) >> /data/file.txt; sleep 5; done"]
        volumeMounts:
        - name: shared-pvc
          mountPath: /data
      - name: multitool
        image: wbitt/network-multitool
        command: ["/bin/sh", "-c"]
        args: ["tail -f /data/file.txt"]
        volumeMounts:
        - name: shared-pvc
          mountPath: /data
      volumes:
      - name: shared-pvc
        persistentVolumeClaim:
          claimName: local-pvc